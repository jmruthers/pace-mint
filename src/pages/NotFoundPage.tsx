import { type ReactNode } from 'react'

export function NotFoundPage(): ReactNode {
  return (
    <main>
      <section>
        <h1>Not found</h1>
        <p>This page does not exist.</p>
      </section>
    </main>
  )
}
