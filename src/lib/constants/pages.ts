/**
 * Page/resource names for RBAC (Standard 03).
 * Use with PagePermissionGuard and nav permissions so the page resource has a single source of truth.
 */
export const MINT_PAGE_NAMES = {
  FINANCIAL_CONTEXT: 'financial-context',
} as const
